#include <stdio.h>
#include <math.h>
int main()
{
  // code write here
  long long a, b, c;
  scanf("%lld", &a);
  scanf("%lld", &b);

  // if (a > b)
  // {
  c = (a + b - 1) / b;
  // }
  printf("%lld", c);
  return 0;
}
